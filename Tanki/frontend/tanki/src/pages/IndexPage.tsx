import { Lobby } from "../components/Lobby";
import { Connection } from "../models/Connection";

interface Props {
    connection: Connection
}

export const IndexPage = ({connection}: Props) =>
{
    return (
        <div>
            
        </div>
    );
}